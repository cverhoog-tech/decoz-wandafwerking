const premiumCarousel=document.querySelector('[data-hero-carousel]');if(premiumCarousel){const slides=[...premiumCarousel.querySelectorAll('.carousel-slide')];const dots=[...premiumCarousel.querySelectorAll('.carousel-dot')];const prev=premiumCarousel.querySelector('[data-carousel-prev]');const next=premiumCarousel.querySelector('[data-carousel-next]');let index=0;let timer;const show=i=>{index=(i+slides.length)%slides.length;slides.forEach((s,n)=>s.classList.toggle('active',n===index));dots.forEach((d,n)=>d.classList.toggle('active',n===index));};const start=()=>timer=setInterval(()=>show(index+1),5200);const reset=()=>{clearInterval(timer);start();};prev?.addEventListener('click',()=>{show(index-1);reset();});next?.addEventListener('click',()=>{show(index+1);reset();});dots.forEach((d,n)=>d.addEventListener('click',()=>{show(n);reset();}));premiumCarousel.addEventListener('mouseenter',()=>clearInterval(timer));premiumCarousel.addEventListener('mouseleave',start);show(0);start();}